import React from "react";

export const QuizzList = ({ topic, title, amount }) => {
  return (
    <div className="quiz-list">
      <p>we have many different quizzes that you can choose from </p>
    </div>
  );
};
